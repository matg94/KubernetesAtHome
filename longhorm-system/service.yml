apiVersion: v1
kind: Service
metadata:
  name: longhorn-frontend
  namespace: longhorn-system
spec:
  type: LoadBalancer
  ports:
    - port: 8085
      name: http
      targetPort: 8000
  selector:
    app: longhorn-ui
