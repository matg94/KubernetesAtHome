apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: radio
  name: openwebrx-config-pvc
spec:
  storageClassName: ssd-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
