apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: fitcoach
  name: fitcoach-postgres-volume-claim
spec:
  storageClassName: ssd-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
