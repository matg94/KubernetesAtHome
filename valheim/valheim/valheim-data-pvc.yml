apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: valheim
  name: valheim-data-claim
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi