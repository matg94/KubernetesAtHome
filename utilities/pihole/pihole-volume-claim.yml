apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: utilities
  name: pihole-volume-claim
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
