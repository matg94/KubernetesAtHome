apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: media
  name: overseerr-config-pvc
spec:
  storageClassName: ssd-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
