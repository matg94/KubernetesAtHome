apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: media
  name: plex-config-pvc
spec:
  storageClassName: ssd-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi